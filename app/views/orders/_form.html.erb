<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id , value: current_user.profile.id%>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select :status, Order.statuses.keys, {}%>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, value: Product.first.id %>
  </div>

  <div class="field">
    <%= form.label :repartidor_id %>
    <%= form.text_field :repartidor_id, value: current_user.profile.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
